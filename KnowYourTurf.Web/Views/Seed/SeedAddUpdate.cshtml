@model KnowYourTurf.Web.Models.SeedViewModel
@using KnowYourTurf.Core
@using KnowYourTurf.Web
@using KnowYourTurf.Core.Html.FubuUI
@using KnowYourTurf.Web.Controllers
@using KnowYourTurf.Core.Html.Expressions

@{
    Layout = "~/Views/Shared/_popupLayout.cshtml";
}

        <div id="puErrorMessages" class="puErrorMessages KYT_errorMessages" ><ul></ul></div>   

            @MvcHtmlString.Create(FormFor<SeedController>(x=>x.Save(null)).Class("CRUD").ElementId("CRUDForm").ToString())
            @MvcHtmlString.Create(Html.HiddenFor(x => x.Seed.EntityId).ToString())
            @MvcHtmlString.Create(Html.SubmissionFor(x => x.Seed.Name).ToString())
            @MvcHtmlString.Create(Html.SubmissionFor(x => x.Seed.Description).ToString())
            @MvcHtmlString.Create(EndForm())

