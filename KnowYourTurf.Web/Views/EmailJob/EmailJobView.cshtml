@model KnowYourTurf.Web.Models.EmailJobViewModel
@using KnowYourTurf.Core
@using KnowYourTurf.Web
@using KnowYourTurf.Core.Html.FubuUI

@{
    Layout = "";
}
        
            @MvcHtmlString.Create(Html.HiddenFor(x => x.EmailJob.EntityId, new { @class = "copyEntityId" }).ToString())
            @MvcHtmlString.Create(Html.HiddenFor(x => x.AddEditUrl).ToString())
            @MvcHtmlString.Create(Html.ViewFor(x => x.EmailJob.Name).ToString())
            @MvcHtmlString.Create(Html.ViewFor(x => x.EmailJob.Description).ToString())
            @MvcHtmlString.Create(Html.ViewFor(x => x.EmailJob.Subject).ToString())
            @MvcHtmlString.Create(Html.ViewFor(x => x.EmailJob.Frequency).ToString())
            @MvcHtmlString.Create(Html.ViewFor(x => x.EmailJob.Status).ToString())

